import React, {Component} from 'react'

// 左侧导航组件
export default class Header extends Component {
    render(){
        return (
            <div>
                Header
            </div>
        )
    }
}